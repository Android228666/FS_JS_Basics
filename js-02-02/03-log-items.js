function logItems(items) {
    for (const item of items) {
        console.log(item);
    }
}

logItems(["Mango", "Kiwi", "Poly", "Ajax"]);
logItems([1, 2, 3, 4, 5]);
logItems(["keyboard", "airpods", "watch"]);
